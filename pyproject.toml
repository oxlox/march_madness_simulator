[project]
name = "march-madness-simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "coverage>=7.7.1",
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]
